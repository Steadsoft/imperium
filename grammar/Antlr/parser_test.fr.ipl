
%lexicon(en); // tells the compiler which lexicon to use for this source file.

/*
procédé French (X)

	déclarer counter binaire(15);	

    counter = FACE B034 D85F BAD7:H;

	si counter > 0 ensuite
	   appeler retour;
	autre
	   retour;
	fin;

	boucle tandis que (a > b)
	   aller à place;
	fin;

fin;
*/

procedure English (X);

	declare counter binary(15);	
 
        counter = FACE B034 D85F BAD7:H;

	if counter > 0 then
	   call retour;
	else
	   return;
	end;

	text = "khsAKJSHakshAJK"
	SHajkhda:QKDHAKSDJAH";

	loop while (a > "aaa")
	   go to place;
	end;
		
end;

/*

procedure Japanese (X)

    // has Object-Verb rather than Verb-Object
	declare counter binary(15);	
 
    counter = FACE B034 D85F BAD7:H;

	if counter > 0 then
	   //call retour;
	   retor call;
	else
	   return;
	end;

	loop while (a > b)
	   //go to place;
	   place goto;
	end;

end;


*/

/*
routine German (X)

	deklarieren counter binär(15);	

    counter = FACE B034 D85F BAD7:H;

	wenn counter > 0 dann
	   aufrufen retour;
	anders
	   rückkehr;
	ende;

	wiederholen solang (a > b)
	   gehen place;
	ende;

ende;


процедура Russian (X)

		объявлять counter бинарный(15);

		counter = FACE B034 D85F BAD7:H;

		если counter > 0 то
		   вызов retour;
		еще
		   возврат;
		конец;

        повторять в то время как (a > b)
			 перейти к place;
		конец;

конец;	
*/